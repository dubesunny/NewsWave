<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsWave" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/indigo</item>
        <item name="colorPrimaryVariant">@color/indigo</item>
        <item name="colorOnPrimary">@color/black</item>

        <!--secondary brand color-->
        <item name="colorSecondary">@color/lightBlue</item>

        <!-- status bar color-->
        <item name="android:statusBarColor">@color/indigo</item>
    </style>

    <style name="Theme.NewsWave" parent="Base.Theme.NewsWave" />
</resources>